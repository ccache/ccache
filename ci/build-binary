#!/bin/sh

set -eu

mkdir -p "build"
cd "build"
cmake \
    -D CMAKE_BUILD_TYPE=Release \
    -D DEPS=DOWNLOAD \
    -D ENABLE_TESTING=OFF \
    -D CMAKE_INSTALL_PREFIX=$(pwd)/../install \
    ..
cmake --build .
cmake --install . --strip
